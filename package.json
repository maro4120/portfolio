{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "serve": "browser-sync start --server --files \"**/*.html, **/*.css, **/*.js\"",
    "sass": "sass --watch sass:css",
    "start": "run-p serve sass"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "npm-run-all": "^4.1.5",
    "sass": "^1.77.0"
  }
}